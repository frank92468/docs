---
title: "快速开始"
sidebarTitle: "快速开始"
description: "5 分钟完成注册并发送第一个 API 请求"
---

## 第一步：注册账号

前往 [SeeOneAPI 控制台](https://seeoneapi.com) 注册账号。

## 第二步：获取 API Key

登录后，在控制台的「令牌」页面创建一个新的 API Key。

<Warning>
  请妥善保管你的 API Key，不要将其提交到公开代码仓库或分享给他人。
</Warning>

## 第三步：充值

在控制台的「充值」页面选择充值金额，支持支付宝和微信支付。新用户首次充值可获赠额度。

## 第四步：发送第一个请求

SeeOneAPI 完全兼容 OpenAI API 格式，只需修改 Base URL 即可。

<CodeGroup>

```bash cURL
curl https://api.seeoneapi.com/v1/chat/completions \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer sk-你的API密钥" \
  -d '{
    "model": "claude-sonnet-4-20250514",
    "messages": [
      {"role": "user", "content": "你好，请介绍一下你自己"}
    ]
  }'
```

```python Python
from openai import OpenAI

client = OpenAI(
    api_key="sk-你的API密钥",
    base_url="https://api.seeoneapi.com/v1"
)

response = client.chat.completions.create(
    model="claude-sonnet-4-20250514",
    messages=[
        {"role": "user", "content": "你好，请介绍一下你自己"}
    ]
)

print(response.choices[0].message.content)
```

```javascript Node.js
import OpenAI from 'openai';

const client = new OpenAI({
  apiKey: 'sk-你的API密钥',
  baseURL: 'https://api.seeoneapi.com/v1',
});

const response = await client.chat.completions.create({
  model: 'claude-sonnet-4-20250514',
  messages: [
    { role: 'user', content: '你好，请介绍一下你自己' }
  ],
});

console.log(response.choices[0].message.content);
```

</CodeGroup>

<Check>
  如果你看到了 Claude 的回复，恭喜你！你已经成功接入 SeeOneAPI。
</Check>

## 下一步

<CardGroup cols={2}>
  <Card title="认证方式" icon="key" href="/authentication">
    了解 API Key 的使用和安全最佳实践
  </Card>
  <Card title="模型目录" icon="list" href="/models/overview">
    查看所有可用模型和参数
  </Card>
  <Card title="图像生成" icon="image" href="/image-generation/gpt-image-2">
    使用 gpt-image-2 生成图像
  </Card>
  <Card title="错误码" icon="circle-exclamation" href="/guides/error-codes">
    了解常见错误及解决方案
  </Card>
</CardGroup>
